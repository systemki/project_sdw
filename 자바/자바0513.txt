변수 : 역할

값을 저장 일반 
주소 저장 참조 

static 클래스 변수 
아니면 그냥 멤버 


클래스 상속 
 부모 클래스 의 멤버 변수/메소드를 자식클래스가 물려 받는것
 부모클래스 모든 멤버변수/메소드를 물려 받을수 없다.
	>접근자가 private이면 물려 받을수 없다.
- 부모 클래스의 멤버 변수/메소드를 선언하지 않아도 사용할수있다.
	자식클래스가 a, 부모클래스 b

	class A extends B{

}
	-A는 B이다가 성립하면 상속이 가능 : is a 관례 
	

부모 클래스 객체 2 = 객체 1;
명시적으로 가능한 경우
부모 클래스의 객체를 자식클래스로변환시키는 경우 명시적으로 
강제 으로 해야 하는데 항상 가능한것 아니다.


instanceof B:A를 B로 타입 변환 할수 있는지 확인, 할수 있으면 결과 값이 ture
없으면 결과값이 false




상속 A는 B이다.
멤버변수 
A는 B를 가지고 있다.

스마트폰은 카메라이다. X > 카메라를 스마트폰에 상속할수가 없다.
스마트폰은 카메라 가지고 있다. O > 카메라는 스마트폰의 멤버 변수 될 수있다.

가계부는 내역이다. 