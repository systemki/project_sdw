package day5;

public class Test5_6 {

	public static void main(String[] args) {
		int r, min = 10, max =1, i=0;
		int [] arr = new int[4];
		r = (int)(Math.random() * (max - min + 1) +min);
		r = (int)(Math.random() * (max - min + 1) +min);
		r = (int)(Math.random() * (max - min + 1) +min);
		r = (int)(Math.random() * (max - min + 1) +min);

		arr[i] = r;
		
		// * 			  i를 변수 gcd에 저장 
		// * 반복문 종료 후 : gcd를 출력  	gcd = i;
		
		System.out.println(r);
		
	}

}
